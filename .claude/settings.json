{
    "permissions": {
        "allow": ["Read", "Glob", "WebFetch"]
    },
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Write|Edit",
                "hooks": [
                    {
                        "type": "command",
                        "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | grep -E '\\\\.ts$' && cd \"G:/Documents/Programming Projects/Playwright/ai automation setup\" && npm run format 2>/dev/null || true",
                        "statusMessage": "Running Prettier..."
                    }
                ]
            }
        ]
    }
}
