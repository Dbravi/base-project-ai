{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Write|Edit",
                "hooks": [
                    {
                        "type": "command",
                        "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | grep -E '\\.ts$' && cd \"G:/Documents/Programming Projects/Playwright/ai automation setup\" && npm run fix 2>/dev/null || true",
                        "statusMessage": "Running ESLint + Prettier..."
                    }
                ]
            }
        ]
    }
}
